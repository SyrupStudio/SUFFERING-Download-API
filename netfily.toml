[build]
  # No special build command needed if you are just running the file
  # If you have a build step (like npm run build), add it here:
  # command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/index"
  status = 200
  
